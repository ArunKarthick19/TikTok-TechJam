# Trained artifacts

Copy these files from the completed DINOv2 SID Colab run into this directory:

- `selected_spatial_probe.joblib`
- `frequency_probe.joblib`
- `selected_config.json`

The inference command validates the files and will not substitute guessed fusion
weights, thresholds, or classifiers when they are missing.
